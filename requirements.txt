# litellm[proxy] 1.95.0 declares `fastapi>=0.136.3,<1.0`, but its proxy imports
# `get_flat_dependant` from fastapi.dependencies.utils, which FastAPI removed in
# 0.141. The declared range therefore allows versions that fail at startup with
# ImportError -- it is missing an upper bound.
#
# 0.140.0 is the newest release that both satisfies LiteLLM's declared floor and
# still provides the symbol, so this resolves in a single pip pass. install.sh
# asserts the import afterwards, since the metadata cannot be trusted to catch
# a regression here.
litellm[proxy]==1.95.0
fastapi==0.140.0

# Router dependencies (also pulled in by litellm[proxy]; pinned for clarity).
uvicorn==0.52.1
httpx==0.28.1
